using System; // Importing the System namespace to access built-in C# functionalities like input/output.

namespace BilalStudio // Defining a namespace called BilalStudio to encapsulate the program.
{
    class Program // Defining a class named Program, which serves as the container for the Main method.
    {
        static void Main(string[] args) // The Main method, the entry point of the program where execution begins.
        {
            Console.WriteLine("Welcome to Bilal Studio (BS)!"); // Using the Console.WriteLine method to print the welcome message to the console.
        }
    }
}
